---
import Layout from '../layouts/Layout.astro';
import InstagramFeed from '../components/InstagramFeed.astro';

const tourDates = [
  {
    date: "28 Marzo 2024",
    venue: "Teatro Olimpico",
    location: "Roma",
    show: "Una Canzone Per Te",
    ticketUrl: "/contatti"
  },
  {
    date: "15 Aprile 2024",
    venue: "Teatro Brancaccio",
    location: "Roma",
    show: "Questione di Feeling",
    ticketUrl: "/contatti"
  },
  {
    date: "5 Maggio 2024",
    venue: "Auditorium Parco della Musica",
    location: "Roma",
    show: "Mercanti di Stelle",
    ticketUrl: "/contatti"
  }
];
---

<Layout title="iMarcos Eventi - Sito Web Ufficiale">
  <main>
    <!-- Hero Section -->
    <section class="relative h-screen flex items-center justify-center">
      <div class="absolute inset-0 bg-gradient-to-b from-transparent to-band-primary">
        <img 
          src="https://raw.githubusercontent.com/tommasop/imarcoseventi/master/public/hero.jpg"
          alt="i Marcos in concerto"
          class="w-full h-full object-cover opacity-70"
        />
      </div>
      <div class="relative text-center">
        <img 
          src="https://raw.githubusercontent.com/tommasop/imarcoseventi/master/public/logo-transparent.png" 
          alt="i Marcos Eventi" 
          class="max-w-[600px] w-full mx-auto mb-8 px-4"
        />
        <a 
          href="/contattaci" 
          class="bg-band-accent hover:bg-band-accent/80 text-white px-8 py-3 rounded-full text-lg font-semibold transition-colors"
        >
          Prenota il tuo Evento
        </a>
      </div>
    </section>

    <!-- Tour Dates Section 
    <section class="py-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-4xl font-bold mb-12 text-center">Prossime Date</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          {tourDates.map((date) => (
            <div class="bg-black/30 rounded-lg p-8 hover:transform hover:scale-105 transition-transform">
              <div class="text-band-accent text-2xl font-bold mb-4">{date.date}</div>
              <h3 class="text-xl font-bold mb-2">{date.show}</h3>
              <p class="text-gray-300 mb-2">{date.venue}</p>
              <p class="text-gray-300 mb-6">{date.location}</p>
              <a 
                href={date.ticketUrl} 
                class="inline-block bg-band-accent hover:bg-band-accent/80 text-white px-6 py-2 rounded-full text-sm font-semibold transition-colors"
              >
                Prenota
              </a>
            </div>
          ))}
        </div>
      </div>
    </section> -->

    <!-- Instagram Feed Section -->
    <section class="bg-black/50 py-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- <h2 class="text-4xl font-bold mb-12 text-center">News</h2> -->
        <InstagramFeed limit={8} columns={4} showCaption={true} showStats={true} />
      </div>
    </section>
  </main>
</Layout>